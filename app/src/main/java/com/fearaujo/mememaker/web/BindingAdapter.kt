package com.fearaujo.mememaker.web

// TODO: Use bindAdapter to load image with picasso

/*
import android.widget.ImageView
import androidx.databinding.BindingAdapter
import com.squareup.picasso.Picasso

@BindingAdapter("imageUrl")
@JvmStatic fun loadImageUrlIntoImageView(imageView: ImageView, url: String) {
    if (url.isNotEmpty()) {
        val picasso = Picasso.Builder(imageView.context).build()
        picasso.load(url).into(imageView)
    }
}*/
